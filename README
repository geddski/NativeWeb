This is an example application demonstrating how to build Native Web applications. To run the build tool cd into components, then run ../../requirejs-0.23.0/build/build.sh app.build.js
this assumes you have node installed. 
